@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="ro">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>📦 Gestiune Colete AWB - Admin</title>
    <link rel="stylesheet" href="~/css/awb-colet.css" />
</head>
<body>
    <div class="container">
        <header>
            <h1>📦 Gestiune Colete AWB</h1>
            <p>Încarcă AWB-uri PDF și extrage automat informațiile</p>
            <nav>
                <a href="/" class="nav-link">📷 Scanare</a>
                <a href="/Istoric" class="nav-link">📋 Istoric</a>
            </nav>
        </header>

        <!-- Upload Zone -->
        <div class="upload-zone" id="uploadZone">
            <div class="upload-icon">📁</div>
            <h2>Trage fișierele PDF aici</h2>
            <p>sau click pentru a selecta fișiere</p>
            <input type="file" id="fileInput" accept=".pdf" multiple>
        </div>

        <!-- Progress -->
        <div id="uploadProgress" class="upload-progress" style="display:none;">
            <div class="spinner"></div>
            <p id="progressText">Se procesează fișierele...</p>
            <div class="progress-bar">
                <div class="progress-fill" id="progressFill"></div>
            </div>
        </div>

        <!-- Upload Results -->
        <div id="uploadResults" class="upload-results" style="display:none;"></div>

        <!-- Stats -->
        <div class="stats-bar">
            <div class="stat-card">
                <div class="value" id="statTotal">0</div>
                <div class="label">Total Colete</div>
            </div>
            <div class="stat-card">
                <div class="value" id="statPending">0</div>
                <div class="label">În așteptare</div>
            </div>
            <div class="stat-card">
                <div class="value" id="statLivrat">0</div>
                <div class="label">Livrate</div>
            </div>
            <div class="stat-card highlight">
                <div class="value" id="statRamburs">0</div>
                <div class="label">Total Ramburs (RON)</div>
            </div>
            <div class="stat-card">
                <div class="value" id="statGreutate">0</div>
                <div class="label">Greutate (kg)</div>
            </div>
        </div>

        <!-- Actions Bar -->
        <div class="actions-bar">
            <div class="search-box">
                <input type="text" id="searchInput" placeholder="🔍 Caută AWB, destinatar, telefon...">
            </div>
            <div class="filter-buttons">
                <button class="filter-btn active" onclick="filterByStatus('')">Toate</button>
                <button class="filter-btn" onclick="filterByStatus('PENDING')">În așteptare</button>
                <button class="filter-btn" onclick="filterByStatus('LIVRAT')">Livrate</button>
                <button class="filter-btn" onclick="filterByStatus('RETURNAT')">Returnate</button>
            </div>
            <div class="action-buttons">
                <button class="btn btn-primary" onclick="exportData()">📥 Export CSV</button>
                <button class="btn btn-danger" onclick="clearAll()">🗑️ Șterge tot</button>
            </div>
        </div>

        <!-- Results Table -->
        <div class="results-section">
            <div class="table-wrapper">
<table id="coleteTable">
<thead>
    <tr>
        <th>#</th>
        <th>Data Creare AWB</th>
        <th>AWB</th>
        <th>Destinatar</th>
        <th>Produse</th>
        <th>Ramburs</th>
        <th>Telefon</th>
        <th>Greutate</th>
        <th>Status</th>
        <th>Acțiuni</th>
    </tr>
</thead>
    <tbody id="coleteBody">
    </tbody>
</table>
            </div>
            <div id="emptyState" class="empty-state">
                <div class="icon">📋</div>
                <p>Încarcă PDF-uri AWB pentru a vedea coletele</p>
            </div>
        </div>
    </div>

    <!-- Detail Modal -->
    <div id="detailModal" class="modal">
        <div class="modal-content">
            <span class="close" onclick="closeModal()">&times;</span>
            <h3>📦 Detalii Colet</h3>
            <div id="modalContent"></div>
        </div>
    </div>

    <script src="~/js/awb-colet.js"></script>
</body>
</html>
